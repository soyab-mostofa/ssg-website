import React from 'react'
import SustainabilityPage from './Client'
import { getPayload } from '@/lib/payload'

const page = async () => {
  return (
    <div>
      <SustainabilityPage />
    </div>
  )
}

export default page
